# Dependencies
node_modules/
vendor/
frontend/node_modules/
frontend/dist/

# Laravel
.env
.env.*
!.env.example
storage/*.key
bootstrap/cache/*
!bootstrap/cache/.gitignore

# IDE / OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# Build artifacts
*.log
.phpunit.result.cache

# Service directories are committed so Render/CI can build Docker images.
# Each service has its own .gitignore that excludes .env, vendor/, etc.

# Legacy flat source files (migrated to service-files/)
/mnt/
