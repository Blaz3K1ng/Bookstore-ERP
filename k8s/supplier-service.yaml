apiVersion: apps/v1
kind: Deployment
metadata:
  name: supplier-service
  namespace: bookstore
  labels:
    app: supplier-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: supplier-service
  template:
    metadata:
      labels:
        app: supplier-service
    spec:
      containers:
        - name: supplier-service
          image: ghcr.io/OWNER/bookstore-supplier:latest
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: bookstore-config
            - secretRef:
                name: bookstore-secrets
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: supplier-db-secret
                  key: DATABASE_URL
          readinessProbe:
            httpGet:
              path: /api/health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 15
          livenessProbe:
            httpGet:
              path: /api/health
              port: 8000
            initialDelaySeconds: 60
            periodSeconds: 30
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: supplier-service
  namespace: bookstore
spec:
  selector:
    app: supplier-service
  ports:
    - port: 80
      targetPort: 8000
